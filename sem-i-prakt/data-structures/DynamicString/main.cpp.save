#include <iostream>
#include "dstring.h"
using namespace std;

int main()
{
    DynamicString ds = "what the fuck", ds1;
    cout << ds << endl;
    cout << (ds.find("the") == DynamicString::npos ? "found at postion " : "not found") << endl;
    getline(cin, ds1);
    cout << ds1 << endl;

	return 0;
}
